import { View, Text } from "react-native";
const select_type_add = () => {
    return (
        <View>
            <Text>select_type_add</Text>
        </View>
    );
};
export default select_type_add;
